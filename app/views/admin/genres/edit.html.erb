<div class="container">
  <div class="row justify-content-center">
    <h3>ジャンル編集</h3>
  </div>
  <div class="row justify-contenet-center">
     <div class="col-md-6 mx-auto">
        <%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch, class: "text-center" do |f| %>
          <%= render 'admin/genres/genre_form', f: f %>
          <div class="form-group">
            <%= f.submit :"変更を保存", class: "btn btn-success" %>
          </div>
        <% end %>
    </div>
  </div>
</div>